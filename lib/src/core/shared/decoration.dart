import 'package:flutter/cupertino.dart';

import '../theme/colors.dart';

Border textBorder = Border.all(color: AppColors.black54, width: 0.4);
List<BoxShadow> bottomNavShadow = [
  BoxShadow(blurRadius: 6, offset: Offset(6, 0), color: AppColors.gray),
  BoxShadow(blurRadius: 6, offset: Offset(8, 0), color: AppColors.gray),
];
